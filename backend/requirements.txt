# FastAPI + ASGI
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.9

# Data layer
sqlalchemy>=2.0
sqlmodel>=0.0.22
aiosqlite>=0.20
greenlet>=3   # required by SQLAlchemy async

# Pydantic
pydantic>=2.9
pydantic-settings>=2.6

# WebSocket
websockets>=13

# Background jobs
anyio>=4

# Reports
weasyprint>=63   # HTML -> PDF for reports
jinja2>=3.1

# Test
pytest>=8
pytest-asyncio>=0.24
httpx>=0.27

# The simulator itself — kept in the parent project's requirements.txt.
# When the backend Docker image is built, it pip-installs both this file
# and /app/requirements.txt to bring in mesa, statsmodels, river, etc.

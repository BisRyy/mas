# Core scientific stack
numpy>=1.26
pandas>=2.2
scipy>=1.13

# Multi-agent simulation framework
mesa>=2.3
# Mesa 3.5+ eagerly imports its discrete_space module on `import mesa`,
# which pulls in networkx unconditionally. We never use discrete_space,
# but the import fires before we get a chance not to, so networkx is a
# de-facto mandatory dependency even though the simulator never builds
# a graph.
networkx>=3.0

# Forecasting
statsmodels>=0.14
scikit-learn>=1.4

# Concept drift detection
river>=0.21      # ADWIN, DDM, EDDM, KSWIN
ruptures>=1.1    # PELT and other change-point detectors

# Configs and CLI
pyyaml>=6.0
click>=8.1

# Reproducibility / logging
tqdm>=4.66
loguru>=0.7

# Plotting / analysis
matplotlib>=3.8
seaborn>=0.13

# Testing
pytest>=8.0
pytest-cov>=5.0

# Notebooks
jupyter>=1.0
nbformat>=5.10
ipykernel>=6.29

# Dashboard
streamlit>=1.36
plotly>=5.20

# Resource profiling (used by sweep)
psutil>=5.9

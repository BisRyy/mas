# Baseline 1: Static Reorder Point — no adaptation, no drift.
name: baseline_rop
seed: 42
n_steps: 365

data:
  processed_path: data/processed/orders.csv

policy: static_rop

drift:
  scenarios: []

results_dir: results
